# PROG-REQ-20260824-007 多引擎独立结果数量与分组展示

## 当前状态

已完成代码实现，待部署环境和真实上游端到端验证。

## 里程碑

- [x] 建立需求与决策记录，确认 nullable 配置、调用方上限和双形态响应。
- [x] 新增 `engine.result_limit` 迁移与 schema 入口。
- [x] 完成按引擎并发、独立缓存、分组响应和历史快照兼容。
- [x] 完成引擎管理数量下拉、首页分组/聚合切换和状态展示。
- [x] 完成 MCP、测试、文档及 lint/typecheck/test/build 验收。

## 风险与边界

Fake upstream 和旧历史快照已覆盖；前端不再保留手动伪造状态控制。失败状态来自真实服务响应，正文读取仍走已有 SSRF 防护。真实 Open-WebSearch 各引擎数量和生产 MCP 客户端仍属于部署阶段回归范围。

## 最新验证记录（2026-08-24）

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 均通过。
- 服务端测试覆盖旧引擎回退、按引擎数量、调用方上限、独立缓存、去重来源、局部/全部失败、配置 API、历史快照和真实 MCP `tools/call` 响应。
- 本地开发服务器已可访问首页；真实上游网络、部署配置和外部 MCP 客户端回归仍需在阶段 4 执行。
