# PROG-REQ-20260829-002：门户正文阅读器、错误本地化、引擎顺序与桌面宽度

## 状态

已完成代码实现，待登录后的浏览器交互回归。

## 已完成

- 正文弹窗增加骨架屏、来源引擎 Tag、最终 URL、新窗口链接、正文 URL 高亮、重试和原始错误详情展开。
- 新增共享前端错误映射，覆盖正文提取失败、无效 URL、上游不可用、Bing 302/Invalid URL、超时、限流和常见服务错误；MCP 返回保持原样。
- 新增首页/MCP 两套引擎顺序设置，后端按 Web/MCP channel 读取，管理页支持拖拽和上下移动并持久化。
- 门户桌面内容宽度统一调整为约 90%。

## 验证

- `pnpm typecheck` 通过。
- `pnpm lint` 通过。
- `pnpm test` 通过（门户契约 20 项，服务端 35 项）。
- 上游保留的通用 `UPSTREAM_UNAVAILABLE` 代码会按消息细分为 302 异常重定向或无效链接摘要；“查看详情”仍展示原始错误。
- 已按 LPK V2 release 配置重新生成 `release/miaomiao-search-0.1.1.lpk`（149,504 bytes，SHA-256：`9d9b1514eb5a38cd43c29a1d2c59f64895594350f44ff0da1cebc69f7de55372`）；`lzc-cli project lint` 无警告。

## 待验证

- 登录后在桌面与移动视口检查弹窗骨架、正文链接新窗口、错误详情切换和两套拖拽顺序刷新恢复。
- 用真实搜索请求确认 MCP 未指定 `engines` 时的顺序与管理页一致。
