# PROG-REQ-20260831-002：门户交互与 B站详情展示

## 当前状态

本地实现与自动化验证完成，待登录后的浏览器回归确认表格下拉、B站详情弹窗和凭据入口的视觉表现。

## 已完成

- Dropdown 改为 Portal 浮层，增加视口边界、滚动和窗口变化定位。
- 正文阅读器内容区增加圆角；B站 Provider 返回封面与可用视频元数据，门户使用独立详情弹窗并隐藏正文复制按钮。
- 引擎目录和管理 API 返回官方凭据申请链接；凭据输入支持明文/脱敏切换，管理 Tag 增加点击修改提示。
- 首页取消最后来源时保留选择并提示；补充 B站元数据 Provider 单测和需求/决策记录。

## 验证证据

- `pnpm typecheck` 通过。
- `pnpm lint` 通过，仅保留已有的 B站详情 `<img>` 性能 warning。
- `pnpm test` 通过：门户契约 22 项，服务端 50 项。

## 待验证

- 登录后在桌面窄表格中打开每引擎数量下拉并滚动页面，确认浮层不被裁剪。
- 启用 B站并搜索，确认详情弹窗显示真实上游返回的可用统计；上游缺字段时显示 `—`。
- 分别打开 Exa、Firecrawl、Tavily、GitHub 凭据弹窗，确认申请链接新窗口打开且输入切换不改变提交值。
