# PROG-REQ-20260825-010：MCP Token 调用量与删除操作

## 当前阶段

已完成，等待交付验证记录归档。

## 已完成

- 服务端按与每日限流相同的 UTC 自然日边界聚合 `request_log.token_id`。
- `/api/tokens` 和前端映射传递真实 `usageToday`，移除固定 `0` 占位值。
- MCP 页面增加删除按钮、二次确认、成功刷新和错误提示；删除后保留审计记录。
- 增加 Token 服务与管理 API 回归测试，覆盖今日计数和删除结果。
+ 已核对历史 LPK 的多实例口径；当前清单已由 REQ-20260826-001 改为单实例，Token 按 owner 绑定并使用安装级密钥。

## 待验证

- 在懒猫设备安装包后，使用真实 OIDC 会话打开 MCP 页面，完成删除弹窗的浏览器交互。
- 部署后的真实外部 MCP 调用继续写入对应实例数据库并在门户刷新后显示。
