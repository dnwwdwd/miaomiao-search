# PROG-REQ-20260825-002 移除 Startpage、网站图标与 JSON 查看

## 当前状态

代码实现、自动化验证和桌面/375px 浏览器回归完成。

## 已完成

- 移除 Startpage 的服务端枚举、旧数据库配置、首页/管理页/示例数据引用。
- 新增删除 Startpage 的数据库迁移，并清空过期的首页引擎偏好。
- 为服务端和门户搜索结果补充可选 `faviconUrl` 类型与派生逻辑。
- 首页结果卡片加入图标回退，成功/部分成功结果可打开嵌套 JSON 查看器，支持一键复制和复制状态反馈。
- 增加 MCP 双形态 JSON/结构化响应的源码契约测试。

## 已验证

- `pnpm lint`、`pnpm typecheck`、`pnpm build` 通过；服务端测试 17/17 通过；`git diff --check` 无格式错误。
- 浏览器验证桌面与 375px 窄屏布局、代码区滚动、复制按钮状态和剪贴板写入，最终控制台无错误或警告。

## 剩余风险

- 真实 MCP 外部客户端的复制/消费链路未在本轮浏览器回归中覆盖，服务端协议契约测试已覆盖文本 JSON 与结构化响应。
- `pnpm lint` 仍有 `.pnpm-patches/open-websearch@2.1.11/build` 下 5 条历史未使用变量 warning，无 lint error。
