# PROG-REQ-20260825-006：LPK 多实例与懒猫 OIDC

## 当前阶段

实施与打包中。

## 已完成

- 确认 LPK V2、多实例、空数据库挂载、OIDC 点击授权和 MCP 网关放行边界。
- 建立 API/Web 双服务清单、内部路径代理、私有 daemon 启动策略与 Linux amd64 构建方案。
- 开始替换本地管理员密码认证为 OIDC 授权码流程，并新增删除 `admin` 表的迁移。

## 剩余

- 完成门户、文档和测试夹具同步。
- 生成 LPK；按用户要求，生成后不运行额外测试、检查或部署验证。
