# PROG-REQ-20260826-001：单实例懒猫用户隔离

## 当前状态

代码已完成，等待懒猫设备安装和双用户切换回归。

## 已完成

- 清单移除多实例声明并改用稳定安装级密钥。
- 新增身份库、按 UID 懒加载用户数据库、Token owner 索引和用户级服务上下文。
- OIDC、本地登录、门户 API、MCP Token 全部接入 UID/owner 校验。
- 前端统一处理认证失效，并增加首次加载、focus、visibilitychange 和 15 秒复核。
- 服务端现有 26 个测试全部通过。

## 待验证

- 懒猫设备上两个账号分别登录，确认用户库、设置、历史和 Token 隔离。
- 切换网关账号后确认旧页面自动回到 /，且登出不会跳转 404。
- 外部 MCP 客户端分别使用两个 owner 的 Token 验证隔离。
